[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bib"
version = "0.1.0"
description = "A reference manager TUI for papis"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Craig Miller" }]
keywords = ["papis", "citations", "bibliography", "tui", "textual", "reference-manager"]
dependencies = [
    "papis==0.15.0",
    "textual>=8,<9",
    "httpx",
    "rapidfuzz",
    "citeproc-py",
]

[project.scripts]
bib = "bib.cli:main"
bib-fetch = "bib.cli:fetch_main"

[tool.hatch.build.targets.wheel]
# Non-.py files inside the package (styles/csl/*.csl + ATTRIBUTION.md) are included automatically.
packages = ["bib"]

[tool.hatch.build.targets.sdist]
include = ["bib", "tests", "README.md", "LICENSE"]
